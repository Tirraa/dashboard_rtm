/* v8 ignore start */
// Stryker disable all

import type { Score } from './Numbers';

export type DocumentHeading = {
  content: string;
  depth: Score;
  slug: string;
};

// Stryker restore all
/* v8 ignore stop */
