/* v8 ignore start */
// Stryker disable all

import type { ReactNode } from 'react';

type CustomCrumb = {
  jsx: ReactNode;
  depth: number;
};

export type CustomCrumbs = CustomCrumb[];

// Stryker restore all
/* v8 ignore stop */
