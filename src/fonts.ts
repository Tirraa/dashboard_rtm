/* v8 ignore start */
// Stryker disable all
import { Inter } from 'next/font/google';

export const fInter = Inter({
  fallback: ['sans-serif'],
  variable: '--font-sans',
  subsets: ['latin']
});
/* v8 ignore stop */
// Stryker restore all
