/* v8 ignore start */
// Stryker disable all

const ELEMENTS_ID = {
  SITEWIDE_NAVBAR: '--sitewide-navbar',
  BODY_CONTAINER: '--top'
} as const;

export default ELEMENTS_ID;

// Stryker restore all
/* v8 ignore stop */
