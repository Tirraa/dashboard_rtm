/* v8 ignore start */
// Stryker disable all

const ELEMENTS_ID = {
  SITEWIDE_NAVBAR: '--sitewide-navbar',
  FOOTER_CONTAINER: '--footer',
  BODY_CONTAINER: '--top',
  MDX_BODY: '--mdx-body'
} as const;

export default ELEMENTS_ID;

// Stryker restore all
/* v8 ignore stop */
