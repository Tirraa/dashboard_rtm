/* v8 ignore start */
// Stryker disable all

import type { NextFont } from 'next/dist/compiled/@next/font';

const fcn = (f: NextFont): string => f.className;
export default fcn;

// Stryker restore all
/* v8 ignore stop */
