/* v8 ignore start */
// Stryker disable all
type TNavbarIconStyle = {
  SIZE_PX_VALUE: number;
};

const NAVBAR_ICON_STYLE: TNavbarIconStyle = {
  SIZE_PX_VALUE: 25
} as const;

export default NAVBAR_ICON_STYLE;
/* v8 ignore stop */
// Stryker restore all
