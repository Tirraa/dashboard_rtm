/* v8 ignore start */
// Stryker disable all
export enum QA_WARNINGS {
  // eslint-disable-next-line no-unused-vars
  IS_A_FALLBACK = 'is-a-fallback'
}

export const DATA_QA_WARNING_DOM_KEY = 'data-qa-warning';
/* v8 ignore stop */
// Stryker restore all
